// Espera a que el contenido del DOM esté completamente cargado
document.addEventListener('DOMContentLoaded', () => {
    // Eliminada toda la lógica de cambio de tema
    // Ahora solo modo oscuro permanente
});
